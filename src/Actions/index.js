
import {
    votePost,
    receiveData,
    deletePosts,
    receivePosts,
    createPosts,
    orderPost,
} from './Posts';

import {
    receiveCategories
} from './Categories'

import {
    receiveComments,
    addComment,
    voteComment,
    deleteComment
} from './Comments';


export {
    voteComment,
    addComment,
    deleteComment,
    receiveComments,
    votePost,
    receiveData,
    deletePosts,
    receivePosts,
    createPosts,
    orderPost,
    receiveCategories
};